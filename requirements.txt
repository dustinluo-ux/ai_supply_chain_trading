# Python 3.12 — install: pip install -r requirements.txt
# After install: python -m spacy download en_core_web_md
#
# Core (src/, scripts/, verify_environment.py)
# numpy: must be <2.3 for numba (pandas-ta); do not upgrade numpy after install
numpy==2.2.6
numba==0.61.2
pandas
PyYAML
python-dotenv
requests

# Technical / backtest (technical_library, feature_factory, weight_model)
pandas-ta
scikit-learn
PyPortfolioOpt
hmmlearn
xgboost==3.2.0
vectorbt==0.28.4

# News / LLM (news_engine, llm_bridge, news sources)
google-genai
pydantic
newsapi-python

# Optional: FinBERT / spacy / dedupe (news_engine.py)
transformers==5.2.0
spacy==3.8.11
python-Levenshtein==0.27.3

# Data / execution
yfinance==1.2.0
tqdm
ib_insync
nest_asyncio
fastparquet==2025.12.0
ruamel.yaml==0.19.1
